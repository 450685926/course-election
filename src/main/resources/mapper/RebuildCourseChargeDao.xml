<?xml key="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.edu.election.dao.RebuildCourseChargeDao">
    <resultMap id="BaseResultMap" type="com.server.edu.election.entity.RebuildCourseCharge">
        <id column="ID_" jdbcType="BIGINT" property="id" />
        <result column="TRAINING_LEVEL_" jdbcType="VARCHAR" property="trainingLevel" />
        <result column="FORM_LEARNING_" jdbcType="VARCHAR" property="formLearning" />
        <result column="IS_CHARGE_" jdbcType="INTEGER" property="isCharge" />
        <result column="UNIT_PRICE_" jdbcType="INTEGER" property="unitPrice" />
    </resultMap>
    <sql id="Base_Column_List">
        ID_, TRAINING_LEVEL_, FORM_LEARNING_, IS_CHARGE_, UNIT_PRICE_
    </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from rebuild_course_charge_t
        where ID_ = #{id,jdbcType=BIGINT}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from rebuild_course_charge_t
        where ID_ = #{id,jdbcType=BIGINT}
    </delete>
    <insert id="insert" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        insert into rebuild_course_charge_t (ID_, TRAINING_LEVEL_, FORM_LEARNING_,
        IS_CHARGE_, UNIT_PRICE_)
        values (#{id,jdbcType=BIGINT}, #{trainingLevel,jdbcType=VARCHAR}, #{formLearning,jdbcType=VARCHAR},
        #{isCharge,jdbcType=INTEGER}, #{unitPrice,jdbcType=INTEGER})
    </insert>
    <insert id="insertSelective" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        insert into rebuild_course_charge_t
        <trim prefix="(" suffix=")" suffixOverrides=",">

            <if test="trainingLevel != null">
                TRAINING_LEVEL_,
            </if>
            <if test="formLearning != null">
                FORM_LEARNING_,
            </if>
            <if test="isCharge != null">
                IS_CHARGE_,
            </if>
            <if test="unitPrice != null">
                UNIT_PRICE_,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">

            <if test="trainingLevel != null">
                #{trainingLevel,jdbcType=VARCHAR},
            </if>
            <if test="formLearning != null">
                #{formLearning,jdbcType=VARCHAR},
            </if>
            <if test="isCharge != null">
                #{isCharge,jdbcType=INTEGER},
            </if>
            <if test="unitPrice != null">
                #{unitPrice,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        update rebuild_course_charge_t
        <set>
            <if test="trainingLevel != null">
                TRAINING_LEVEL_ = #{trainingLevel,jdbcType=VARCHAR},
            </if>
            <if test="formLearning != null">
                FORM_LEARNING_ = #{formLearning,jdbcType=VARCHAR},
            </if>
            <if test="isCharge != null">
                IS_CHARGE_ = #{isCharge,jdbcType=INTEGER},
            </if>
            <if test="unitPrice != null">
                UNIT_PRICE_ = #{unitPrice,jdbcType=INTEGER},
            </if>
        </set>
        where ID_ = #{id,jdbcType=BIGINT}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        update rebuild_course_charge_t
        set TRAINING_LEVEL_ = #{trainingLevel,jdbcType=VARCHAR},
        FORM_LEARNING_ = #{formLearning,jdbcType=VARCHAR},
        IS_CHARGE_ = #{isCharge,jdbcType=INTEGER},
        UNIT_PRICE_ = #{unitPrice,jdbcType=INTEGER}
        where ID_ = #{id,jdbcType=BIGINT}
    </update>

    <select id="findCourseCharge" parameterType="com.server.edu.election.entity.RebuildCourseCharge" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List"></include>
        from rebuild_course_charge_t
        <where>
            <if test="trainingLevel !=null and trainingLevel !='' ">
                and TRAINING_LEVEL_ =#{trainingLevel,jdbcType=VARCHAR}
            </if>
            <if test="formLearning !=null and formLearning !='' ">
                and FORM_LEARNING_ =#{formLearning,jdbcType=VARCHAR}
            </if>
        </where>
        order by ID_ DESC
    </select>

    <delete id="deleteCourseCharge" parameterType="java.util.List">
        delete from rebuild_course_charge_t
        where ID_ in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <insert id="addCourseStudentToRecycle" parameterType="java.util.List">
        insert into rebuild_course_recycle_t(CALENDAR_ID_,STUDENT_CODE_,COURSE_CODE_)
        VALUES
        <foreach item="item" collection="list" index="index"
                 separator=",">
            (
            #{item.calendarId,jdbcType=BIGINT},
            #{item.studentCode,jdbcType=VARCHAR},
            #{item.code,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

    <select id="findRecycleCourse" parameterType="com.server.edu.election.dto.RebuildCoursePaymentCondition" resultType="com.server.edu.election.entity.RebuildCourseNoChargeList">
        SELECT re.ID_ id,re.CALENDAR_ID_ calendarId,re.STUDENT_CODE_ studentCode,st.NAME_ studentName,co.ID_ courseId,re.COURSE_CODE_ code,co.NAME_ codeName,co.LABEL_ label,co.CREDITS_ credits FROM `rebuild_course_recycle_t` re LEFT JOIN student_t st on re.STUDENT_CODE_=st.STUDENT_CODE_
        LEFT JOIN course_t co on co.CODE_=re.COURSE_CODE_
        <where>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(calendarId)">
                and re.CALENDAR_ID_ = #{calendarId,jdbcType=BIGINT}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(studentCode)">
                and st.STUDENT_CODE_ like CONCAT('%', #{studentCode,jdbcType=VARCHAR}, '%')
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(studentName)">
                and st.NAME_ like CONCAT('%', #{studentName,jdbcType=VARCHAR}, '%')
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(grade)">
                and st.GRADE_ =#{grade,jdbcType=INTEGER}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(trainingLevel)">
                and st.TRAINING_LEVEL_ =#{trainingLevel,jdbcType=VARCHAR}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(studentCategory)">
                and st.STUDENT_CATEGORY_ =#{studentCategory,jdbcType=VARCHAR}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(faculty)">
                and st.FACULTY_ =#{faculty,jdbcType=VARCHAR}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(profession)">
                and st.PROFESSION_ =#{profession,jdbcType=VARCHAR}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(researchDirection)">
                and st.RESEARCH_DIRECTION_ =#{researchDirection,jdbcType=VARCHAR}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(openFaculty)">
                and co.COLLEGE_ =#{openFaculty,jdbcType=VARCHAR}
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(label)">
                and co.LABEL_ like CONCAT('%', #{label,jdbcType=VARCHAR}, '%')
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(courseId)">
                and co.ID_ like CONCAT('%', #{courseId,jdbcType=BIGINT}, '%')
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(code)">
                and co.CODE_ like CONCAT('%', #{code,jdbcType=VARCHAR}, '%')
            </if>
            <if test="@com.server.edu.util.MybatisUtil@isNotEmpty(name)">
                and co.NAME_ like CONCAT('%', #{name,jdbcType=VARCHAR}, '%')
            </if>
        </where>
    </select>

    <delete id="recoveryDataFromRecycleCourse" parameterType="java.util.List">
        delete from rebuild_course_recycle_t
        where ID_ in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </delete>
</mapper>