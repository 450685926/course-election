<?xml key="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.edu.election.dao.RebuildCourseChargeDao">
    <resultMap id="BaseResultMap" type="com.server.edu.election.entity.RebuildCourseCharge">
        <id column="ID_" jdbcType="BIGINT" property="id" />
        <result column="TRAINING_LEVEL_" jdbcType="VARCHAR" property="trainingLevel" />
        <result column="FORM_LEARNING_" jdbcType="VARCHAR" property="formLearning" />
        <result column="IS_CHARGE_" jdbcType="INTEGER" property="isCharge" />
        <result column="UNIT_PRICE_" jdbcType="INTEGER" property="unitPrice" />
    </resultMap>
    <sql id="Base_Column_List">
        ID_, TRAINING_LEVEL_, FORM_LEARNING_, IS_CHARGE_, UNIT_PRICE_
    </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from rebuild_course_charge_t
        where ID_ = #{id,jdbcType=BIGINT}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from rebuild_course_charge_t
        where ID_ = #{id,jdbcType=BIGINT}
    </delete>
    <insert id="insert" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        insert into rebuild_course_charge_t (ID_, TRAINING_LEVEL_, FORM_LEARNING_,
        IS_CHARGE_, UNIT_PRICE_)
        values (#{id,jdbcType=BIGINT}, #{trainingLevel,jdbcType=VARCHAR}, #{formLearning,jdbcType=VARCHAR},
        #{isCharge,jdbcType=INTEGER}, #{unitPrice,jdbcType=INTEGER})
    </insert>
    <insert id="insertSelective" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        insert into rebuild_course_charge_t
        <trim prefix="(" suffix=")" suffixOverrides=",">

            <if test="trainingLevel != null">
                TRAINING_LEVEL_,
            </if>
            <if test="formLearning != null">
                FORM_LEARNING_,
            </if>
            <if test="isCharge != null">
                IS_CHARGE_,
            </if>
            <if test="unitPrice != null">
                UNIT_PRICE_,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">

            <if test="trainingLevel != null">
                #{trainingLevel,jdbcType=VARCHAR},
            </if>
            <if test="formLearning != null">
                #{formLearning,jdbcType=VARCHAR},
            </if>
            <if test="isCharge != null">
                #{isCharge,jdbcType=INTEGER},
            </if>
            <if test="unitPrice != null">
                #{unitPrice,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        update rebuild_course_charge_t
        <set>
            <if test="trainingLevel != null">
                TRAINING_LEVEL_ = #{trainingLevel,jdbcType=VARCHAR},
            </if>
            <if test="formLearning != null">
                FORM_LEARNING_ = #{formLearning,jdbcType=VARCHAR},
            </if>
            <if test="isCharge != null">
                IS_CHARGE_ = #{isCharge,jdbcType=INTEGER},
            </if>
            <if test="unitPrice != null">
                UNIT_PRICE_ = #{unitPrice,jdbcType=INTEGER},
            </if>
        </set>
        where ID_ = #{id,jdbcType=BIGINT}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.server.edu.election.entity.RebuildCourseCharge">
        update rebuild_course_charge_t
        set TRAINING_LEVEL_ = #{trainingLevel,jdbcType=VARCHAR},
        FORM_LEARNING_ = #{formLearning,jdbcType=VARCHAR},
        IS_CHARGE_ = #{isCharge,jdbcType=INTEGER},
        UNIT_PRICE_ = #{unitPrice,jdbcType=INTEGER}
        where ID_ = #{id,jdbcType=BIGINT}
    </update>

    <select id="findCourseCharge" parameterType="com.server.edu.election.entity.RebuildCourseCharge" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List"></include>
        from rebuild_course_charge_t
        <where>
            <if test="trainingLevel !=null and trainingLevel !='' ">
                and TRAINING_LEVEL_ =#{trainingLevel,jdbcType=VARCHAR}
            </if>
            <if test="formLearning !=null and formLearning !='' ">
                and FORM_LEARNING_ =#{formLearning,jdbcType=VARCHAR}
            </if>
        </where>
        order by ID_ DESC
    </select>

    <delete id="deleteCourseCharge" parameterType="java.util.List">
        delete from rebuild_course_charge_t
        where ID_ in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>